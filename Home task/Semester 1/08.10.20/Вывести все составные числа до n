#include <iostream>  

//Вывсти на экран все составные числа до n

using namespace std;

int main()
{
	int n;

	cin >> n; 

	bool* arr = new bool[n];

	for (int i = 0; i < n; i++)
		arr[i] = false;
 

	for (int i = 2; i < n; i++)
		if (arr[i] != true)
			for (int j = i*2; j < n; j +=i)
				arr[j] = true;
	
	for (int i = 2; i < n; i++)
		if (arr[i] != false)
			cout << i << " ";

	delete [] arr;

	return 0;
} 
